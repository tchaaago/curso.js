function soma(n1=0, n2=0){ //definindo que a variável como 0 ajuda ao javascript ler um dos dois como zero e fazer a soma com o valor declarado sem acusar NaN(Not a Number)
    return n1 + n2;
}

console.log(soma(7))